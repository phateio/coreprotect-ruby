en:
  logger_format: "[%<timestamp>s]%<message>s\n"
  estimated_record_deletion_message: "Estimated %<count>s rows to be scan."
  purged_rows_message: "Purged total %<count>s rows."
  would_purge_rows_message: "Would purge total %<count>s rows."
  resume_notice_message: "Resume your work by appending `--start=%<timestamp>d` (%<human_time>s) to the command."
  estimated_orphaned_scan_message: "Estimated %<count>s entity rows to be scanned for orphans."
  purged_orphaned_entities_message: "Purged total %<count>s orphaned entity rows."
  would_purge_orphaned_entities_message: "Would purge total %<count>s orphaned entity rows."
  resume_orphaned_notice_message: "Resume your work by appending `--start=%<rowid>d` to the command."
  estimated_trim_scan_message: "Estimated %<count>s rows to be scanned for hot coordinates."
  trim_nothing_message: "No new rows to scan; the checkpoint is already up to date."
  trim_coordinate_message: "Trimming %<count>d of %<total>d rows at %<world>s (%<x>d, %<y>d, %<z>d) action=%<action>s, keeping the newest %<keep>d."
  trim_dry_run_coordinate_message: "Would trim %<count>d of %<total>d rows at %<world>s (%<x>d, %<y>d, %<z>d) action=%<action>s, keeping the newest %<keep>d."
  trimmed_rows_message: "Trimmed total %<count>s rows."
  would_trim_rows_message: "Would trim total %<count>s rows."
  trim_checkpoint_message: "Checkpoint saved at rowid %<rowid>d."
  trim_state_missing_message: "No checkpoint found at db/trim_state.yml; specify `--start=ROWID` for the first run."
  trim_kill_notice_message: "Trimming `kill` rows may leave orphaned entities; run `co:purge_orphaned_entities` to clean them up."
  # Prompts retained for the legacy block:purge rake task; co.thor commands no longer prompt.
  record_deletion_prompt: "Estimated %<count>s rows to be scan, are you sure you want to continue?"
  prompt_canceled_message: "Canceled tasks."
